# Slack 운영 허브 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `~/.claude/slack.md` 신설 + 라우팅/빌드/스킬 3곳 연쇄 업데이트 → GitHub 통합본 재빌드까지.

**Architecture:** 라우팅 허브 패턴 — 원본 4곳(rules/skill/agent/env)은 불변, 신규 허브 1개가 링크·방향성·로드맵만 제공. CLAUDE.md §2 라우팅 맵 + `build-integrated_v1.sh` FILES 배열 + system-docs-sync SKILL.md 관리 표에 slack.md 등록해 자동 concat 파이프라인에 편입.

**Tech Stack:** Bash (build script), Markdown (hub/spec/skill), Git (원본 저장소), GitHub raw URL (통합본 배포).

**스펙 참조:** `docs/superpowers/specs/2026-04-16-slack-hub-design.md` v2

---

## File Structure

| 액션 | 파일 | 책임 |
|---|---|---|
| Create | `~/.claude/slack.md` | 슬랙 운영 허브 (라우팅 + 채널 지도 + 로드맵) |
| Modify | `~/.claude/CLAUDE.md` | §1 "7개→8개" + §2 라우팅 맵 1행 |
| Modify | `~/.claude/code/build-integrated_v1.sh` | FILES 배열 + 헤더 printf 카운트 |
| Modify | `~/.claude/skills/system-docs-sync/SKILL.md` | 관리 표에 slack.md 등록 + "7개→8개" |

**작업 순서 근거**: (1) slack.md 먼저 만들어야 빌드 스크립트 존재 검증 통과 → (2) CLAUDE.md 라우팅 먼저 업데이트 → (3) build script 업데이트 → (4) system-docs-sync 업데이트 → (5) 전체 빌드 push → (6) 검증.

---

## Task 1: Create `~/.claude/slack.md` (Hub File)

**Files:**
- Create: `/Users/ihyeon-u/.claude/slack.md`

- [ ] **Step 1.1: Write slack.md with 7 sections**

파일 내용 (스펙 §3 기반):

```markdown
# slack.md — 슬랙 운영 허브

**버전**: v1 | **업데이트**: 2026-04-16
**적용**: Claude Code + Claude.ai + haemilsia-bot (Railway)

> **라우팅 허브**. 슬랙 관련 자산 4곳 + 채널 지도 + 방향성 + 확장 로드맵을 한 눈에.

---

## 1. 개요

- **사용 철학**: 수신(알림) · 발신(명령) · 공유(학습) · 수집(정보) 4축
- **허브 역할**: 라우팅 + 채널 지도 + 방향성 + 확장 로드맵
- **원본 위치**: 4곳 — 이 허브는 링크만 (§4 참조)

---

## 2. 채널 지도

| 채널 | ID | 종류 | 방향 | 용도 | 담당 에이전트 | 발송 트리거 |
|---|---|---|---|---|---|---|
| #general-mode | `C0AEM5EJ0ES` | private | 봇→대표 | 작업일지 | slack-courier | 세션 종료 / 에러 해결 |
| #claude-study | `C0AEM59BCKY` | public | 봇→대표 | 복습 카드 | 복습카드관 → slack-courier | task-routine 트리거 |
| *(해밀시아)* | *예약* | — | 봇→대표 | rental-inspection 결과 | haemilsia-bot | 일일 cron (외부) |
| #news-realestate | *예약* | — | 봇→대표 | 부동산 뉴스 | *Phase 1 미정* | 매일 cron |
| #news-tech | *예약* | — | 봇→대표 | IT 뉴스 | *Phase 1 미정* | 매일 cron |
| #bot-commands | *예약* | — | 양방향 | 원격 명령 I/O | haemilsia-bot | 대표님 명령 시 |

**방향 범례**: `봇→대표`(자동 알림) · `대표→봇`(수동 명령) · `양방향`(명령/응답 루프)

---

## 3. 📱 모바일 수신 허브 (Phase 0 — 상시)

외부에서도 업무 흐름 파악 가능하도록 슬랙 모바일 알림 가이드.

- `#general-mode`: 모든 메시지 알림 (작업 완료/에러 즉시 확인)
- `#claude-study`: 멘션+키워드만 (학습은 여유 시)
- 업무 외 시간: "방해 금지" 스케줄 활성화
- 위젯: 홈 화면 "Direct Messages" 위젯 추가
- Phase 2 원격 명령 연결 시 자연스러운 진입점

---

## 4. 현재 운영 항목 (링크만)

| 항목 | 1줄 설명 | 상세 링크 |
|---|---|---|
| 작업일지 | 세션 종료 → #general-mode | `docs/rules/slack-worklog.md` |
| 복습 카드 | task 사이클 완료 → #claude-study | `docs/rules/task-routine.md` |
| 에러 알림 | 에러 → Notion + 슬랙 공지 | `docs/rules/error-handling.md` |
| 슬랙배달관 | v2 신호등 포맷 배달자 | `agents/slack-courier.md` |
| 브리핑 빌더 | 일일 정보 브리핑 봇 템플릿 | `skills/slack-info-briefing-builder/` |
| 브리핑 포맷 | 원라이너/3줄/풀 (쉬운 설명) | `briefing.md` |

---

## 5. 포맷 참조

포맷 예시는 원본에 이미 존재 — 허브는 "어디 있나"만:
- v2 신호등 → `agents/slack-courier.md`
- 복습 카드 → `docs/rules/task-routine.md`
- 에러 알림 → `docs/rules/error-handling.md`
- 브리핑 → `briefing.md`

---

## 6. 금지 패턴

- 이모지 과남용 (섹션당 3개 이내)
- 봇 스팸: 세션당 5건 초과 시 묶어 발송 (임계치 튜닝 중)
- 원본 내용 허브에 복사 금지 (링크만)

---

## 7. 🔮 확장 로드맵

**Phase 이관 규칙**: 별도 스펙으로 분리되어 본격 구현되면 허브에는 **1줄 요약 + 링크만**.

### Phase 1: 매일 뉴스 브리핑 (부동산 + IT)
- **목표**: 매일 09:00 부동산/IT 뉴스 자동 전송
- **활용**: `slack-info-briefing-builder` 스킬 + `haemilsia-bot` Railway cron
- **채널**: `#news-realestate`, `#news-tech`
- **상태**: 아이디어 단계 (허브 완성 후 별도 스펙)

### Phase 2: 슬랙 → Claude Code 원격 명령
- **목표**: 슬랙 `@봇 임대점검 돌려줘` → Claude Code 자동 실행 → 슬랙 응답
- **스케치**:
  ```
  Slack 명령 → haemilsia-bot (Bolt)
             → Claude Agent SDK / RemoteTrigger
             → Claude Code 세션 실행 → 결과 → 슬랙 채널 응답
  ```
- **이슈**: 인증, 명령 화이트리스트, 응답 채널, 타임아웃
- **상태**: 아이디어 단계 (Phase 1 완료 후)

---

## 오픈 이슈

- 업무 시간 외 발송 금지 시간대 (초안: 23:00~08:00) — 운영 데이터 누적 후 결정
- "세션당 5건" 임계치 적정성 — Phase 1 전 조정
- 세션 경계 측정 주체 (slack-courier 인지 방법) — 제한 구현 시
- 해밀시아 rental-inspection 알림 현재 채널 — 확인 필요
- 허브 분할 임계점: **400줄 초과 시** `docs/slack/` 분할 검토

---

*~/.claude/slack.md | 2026-04-16 | v1 — 신설*
```

- [ ] **Step 1.2: Verify link integrity**

Run:
```bash
cd ~/.claude && grep -oE '`[a-zA-Z0-9_/.-]+\.md`|`[a-zA-Z0-9_/-]+/`' slack.md \
  | tr -d '`' \
  | while read p; do [ -e "$p" ] || echo "BROKEN: $p"; done
```
Expected: **no output** (모든 링크 유효) 또는 예약 슬롯 관련 false positive만.

- [ ] **Step 1.3: Commit**

```bash
cd ~/.claude && git add slack.md && git commit -m "$(cat <<'EOF'
feat(slack): 슬랙 운영 허브 신설 (slack.md v1)

4곳 분산 자산 라우팅 허브 — 채널 지도 + 방향성 + Phase 0-2 로드맵.
Phase 1(뉴스 브리핑), Phase 2(원격 명령) 슬롯 예약.

Ref: docs/superpowers/specs/2026-04-16-slack-hub-design.md v2

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: Update `CLAUDE.md` (Routing + Count)

**Files:**
- Modify: `/Users/ihyeon-u/.claude/CLAUDE.md` line 35 (concat count) + §2 라우팅 맵

- [ ] **Step 2.1: Update §1 concat count**

Line 35 현재:
```
| Claude.ai | **GitHub raw URL 통합본** — `...INTEGRATED.md` (7개 md 자동 concat, 5분 캐시) |
```

→ `7개 md` 를 `8개 md` 로 변경:

Use `Edit` tool:
- `old_string`: `(7개 md 자동 concat, 5분 캐시)`
- `new_string`: `(8개 md 자동 concat, 5분 캐시)`

- [ ] **Step 2.2: Add routing row for "슬랙"**

§2 "파일 라우팅 맵" 표의 마지막(`항상 (기본)` 행 바로 위)에 아래 행 추가:

Use `Edit` tool:
- `old_string`:
```
| "설명해줘", "쉽게 풀어줘", "쉽게 설명해줘", "비유로 설명", "무슨 말이야?", "다시 설명" | `briefing.md` | 쉬운 설명 브리핑 (수동 재설명) |
| 항상 (기본) | `CLAUDE.md` | 이 지침의 로컬 버전 |
```
- `new_string`:
```
| "설명해줘", "쉽게 풀어줘", "쉽게 설명해줘", "비유로 설명", "무슨 말이야?", "다시 설명" | `briefing.md` | 쉬운 설명 브리핑 (수동 재설명) |
| "슬랙", "slack", "채널", "브리핑 채널" | `slack.md` | 슬랙 운영 허브 (채널 지도 + 로드맵) |
| 항상 (기본) | `CLAUDE.md` | 이 지침의 로컬 버전 |
```

- [ ] **Step 2.3: Verify edits applied**

Run:
```bash
grep -n "8개 md 자동 concat" ~/.claude/CLAUDE.md
grep -n "슬랙.*slack.md" ~/.claude/CLAUDE.md
```
Expected: 각각 1행 이상 출력.

- [ ] **Step 2.4: Commit**

```bash
cd ~/.claude && git add CLAUDE.md && git commit -m "$(cat <<'EOF'
docs(claude): slack.md 라우팅 편입 + concat 카운트 8개 갱신

§1: 7개 md → 8개 md
§2: "슬랙/slack/채널" 키워드 트리거 추가

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Update `build-integrated_v1.sh` (FILES Array)

**Files:**
- Modify: `/Users/ihyeon-u/.claude/code/build-integrated_v1.sh`
  - Line 27 comment
  - Lines 28-36 FILES 배열
  - Lines 50-62 헤더 printf (목차 카운트)

- [ ] **Step 3.1: Add slack.md to FILES array**

Use `Edit` tool:
- `old_string`:
```
# 7개 원본 파일: "filename|section_title" 형식
FILES=(
  "CLAUDE.md|📘 1. CLAUDE.md — 라우팅 허브"
  "rules.md|📘 2. rules.md — 하위원칙 + 자주 실수 패턴"
  "session.md|📘 3. session.md — 세션 시작/종료 루틴"
  "env-info.md|📘 4. env-info.md — 환경/MCP/Notion ID/배포 인프라"
  "skill-guide.md|📘 5. skill-guide.md — 스킬 가이드"
  "agent.md|📘 6. agent.md — 팀 에이전트 레지스트리"
  "briefing.md|📘 7. briefing.md — 쉬운 설명 브리핑"
)
```
- `new_string`:
```
# 8개 원본 파일: "filename|section_title" 형식
FILES=(
  "CLAUDE.md|📘 1. CLAUDE.md — 라우팅 허브"
  "rules.md|📘 2. rules.md — 하위원칙 + 자주 실수 패턴"
  "session.md|📘 3. session.md — 세션 시작/종료 루틴"
  "env-info.md|📘 4. env-info.md — 환경/MCP/Notion ID/배포 인프라"
  "skill-guide.md|📘 5. skill-guide.md — 스킬 가이드"
  "agent.md|📘 6. agent.md — 팀 에이전트 레지스트리"
  "briefing.md|📘 7. briefing.md — 쉬운 설명 브리핑"
  "slack.md|📘 8. slack.md — 슬랙 운영 허브"
)
```

- [ ] **Step 3.2: Update TOC printf (목차 + 섹션 카운트 주석)**

현재 line 50-62 TOC는 6개만 나열 + briefing.md·slack.md 누락 상태. 8개로 통일:

Use `Edit` tool:
- `old_string`:
```
  printf '## 📑 목차\n'
  printf '1. **CLAUDE.md** — 라우팅 허브 (역할 + 도구 계층 + 파일 라우팅 + 모드 시스템)\n'
  printf '2. **rules.md** — 하위원칙 + 자주 실수 패턴\n'
  printf '3. **session.md** — 세션 시작/종료 루틴\n'
  printf '4. **env-info.md** — 환경/MCP/Notion ID/배포 인프라\n'
  printf '5. **skill-guide.md** — 전체 스킬 목록 + 추천 규칙\n'
  printf '6. **agent.md** — 팀 에이전트 레지스트리\n\n'
```
- `new_string`:
```
  printf '## 📑 목차\n'
  printf '1. **CLAUDE.md** — 라우팅 허브 (역할 + 도구 계층 + 파일 라우팅 + 모드 시스템)\n'
  printf '2. **rules.md** — 하위원칙 + 자주 실수 패턴\n'
  printf '3. **session.md** — 세션 시작/종료 루틴\n'
  printf '4. **env-info.md** — 환경/MCP/Notion ID/배포 인프라\n'
  printf '5. **skill-guide.md** — 전체 스킬 목록 + 추천 규칙\n'
  printf '6. **agent.md** — 팀 에이전트 레지스트리\n'
  printf '7. **briefing.md** — 쉬운 설명 브리핑\n'
  printf '8. **slack.md** — 슬랙 운영 허브\n\n'
```

- [ ] **Step 3.3: Update section body comment + header text**

Use `Edit` tool:
- `old_string`: `  # 7개 섹션 본문`
- `new_string`: `  # 8개 섹션 본문`

Use `Edit` tool:
- `old_string`: `  printf '> **이 파일은 6개 시스템 문서의 자동 빌드 통합본입니다.**\n'`
- `new_string`: `  printf '> **이 파일은 8개 시스템 문서의 자동 빌드 통합본입니다.**\n'`

- [ ] **Step 3.3b: Update line 2 file-level header comment**

Use `Edit` tool:
- `old_string`: `# build-integrated_v1.sh — 6개 시스템 문서를 하나의 통합본(INTEGRATED.md)으로 concat`
- `new_string`: `# build-integrated_v1.sh — 8개 시스템 문서를 하나의 통합본(INTEGRATED.md)으로 concat`

Use `Edit` tool:
- `old_string`: `#       통합본을 자동 생성/동기화. 원본은 Git의 개별 md 파일 (Single Source of Truth).`
- `new_string`: `#       통합본을 자동 생성/동기화. 원본은 Git의 개별 md 파일 (Single Source of Truth).
#
# v1.2 | 2026-04-16 | slack.md 추가 (7개→8개) + briefing.md TOC 누락 보정`

- [ ] **Step 3.4: Dry-run build (no --push)**

```bash
bash ~/.claude/code/build-integrated_v1.sh
```
Expected output:
```
✅ INTEGRATED.md 빌드 완료
   경로: /Users/ihyeon-u/.claude/INTEGRATED.md
   크기: [숫자] bytes
   줄 수: [숫자] lines
   빌드 시각: ...
```
줄 수가 이전 빌드보다 **증가**해야 함 (slack.md 추가분).

- [ ] **Step 3.5: Verify INTEGRATED.md contains slack.md**

```bash
grep -c "8. slack.md — 슬랙 운영 허브" ~/.claude/INTEGRATED.md
grep -c "📱 모바일 수신 허브" ~/.claude/INTEGRATED.md
```
Expected: 둘 다 **1 이상**.

- [ ] **Step 3.6: Commit build script change**

```bash
cd ~/.claude && git add code/build-integrated_v1.sh INTEGRATED.md && git commit -m "$(cat <<'EOF'
build(integrated): FILES 배열에 slack.md 추가 (7→8개 concat)

목차·주석·헤더 문구도 8개 기준으로 갱신. briefing.md 누락이었던
기존 TOC도 함께 정상화.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Update `system-docs-sync` SKILL.md (Hub Registry)

**Files:**
- Modify: `/Users/ihyeon-u/.claude/skills/system-docs-sync/SKILL.md`

- [ ] **Step 4.1: Update description count (line 4)**

Use `Edit` tool:
- `old_string`: `  시스템 문서(CLAUDE.md, rules.md, session.md, checklist.md, env-info.md, skill-guide.md, agent.md)를`
- `new_string`: `  시스템 문서(CLAUDE.md, rules.md, session.md, env-info.md, skill-guide.md, agent.md, briefing.md, slack.md)를`

(주: checklist.md는 2026-04-12 삭제됨. description도 현실에 맞게 briefing.md + slack.md로 갱신.)

- [ ] **Step 4.1b: Remove checklist.md trigger keyword (line 8)**

Use `Edit` tool:
- `old_string`: `  "CLAUDE.md 수정", "session.md 수정", "checklist.md 수정", "env-info.md 수정",`
- `new_string`: `  "CLAUDE.md 수정", "session.md 수정", "env-info.md 수정",`

- [ ] **Step 4.2: Update managed files section header**

Use `Edit` tool:
- `old_string`: `## 관리 대상: 7개 시스템 문서`
- `new_string`: `## 관리 대상: 8개 시스템 문서`

- [ ] **Step 4.3: Replace managed files table**

Use `Edit` tool:
- `old_string`:
```
| # | 파일 | 내용 |
|---|------|------|
| 1 | `~/.claude/CLAUDE.md` | 핵심 원칙 + 역할 + 도구 계층 + 파일 라우팅 |
| 2 | `~/.claude/rules.md` | 하위원칙 + 자주 실수 패턴 |
| 3 | `~/.claude/session.md` | 세션 시작/종료 루틴 + 기록 규칙 |
| 4 | `~/.claude/checklist.md` | 업무 체크리스트 + 오류 대응표 |
| 5 | `~/.claude/env-info.md` | MCP·환경·Notion ID·명령어 |
| 6 | `~/.claude/skill-guide.md` | 전체 스킬 목록 + 추천 규칙 |
| 7 | `~/.claude/agent.md` | 에이전트 레지스트리 + 계획 시스템 |
```
- `new_string`:
```
| # | 파일 | 내용 |
|---|------|------|
| 1 | `~/.claude/CLAUDE.md` | 핵심 원칙 + 역할 + 도구 계층 + 파일 라우팅 |
| 2 | `~/.claude/rules.md` | 하위원칙 + 자주 실수 패턴 |
| 3 | `~/.claude/session.md` | 세션 시작/종료 루틴 + 기록 규칙 |
| 4 | `~/.claude/env-info.md` | MCP·환경·Notion ID·명령어 |
| 5 | `~/.claude/skill-guide.md` | 전체 스킬 목록 + 추천 규칙 |
| 6 | `~/.claude/agent.md` | 에이전트 레지스트리 + 계획 시스템 |
| 7 | `~/.claude/briefing.md` | 쉬운 설명 브리핑 (원라이너/3줄/풀) |
| 8 | `~/.claude/slack.md` | 슬랙 운영 허브 (채널 지도 + 로드맵) |
```

- [ ] **Step 4.4: Update "1~7번 자동 concat" reference (line 40)**

Use `Edit` tool:
- `old_string`: `| **원본 파일** | `~/.claude/INTEGRATED.md` (1~7번 자동 concat) |`
- `new_string`: `| **원본 파일** | `~/.claude/INTEGRATED.md` (1~8번 자동 concat) |`

- [ ] **Step 4.5: Update Step 3 references (lines 101, 108)**

Use `Edit` tool:
- `old_string`: `1~7번 중 **어느 하나라도 수정**되면 빌드 스크립트 실행:`
- `new_string`: `1~8번 중 **어느 하나라도 수정**되면 빌드 스크립트 실행:`

Use `Edit` tool:
- `old_string`: `1. 7개 원본 파일 존재 검증`
- `new_string`: `1. 8개 원본 파일 존재 검증`

- [ ] **Step 4.5b: Remove stale checklist.md routing row (line 56)**

Use `Edit` tool:
- `old_string`: `| 체크리스트, 오류 대응, task 목록 | checklist.md |
`
- `new_string`: ``

(행 전체 삭제 — checklist.md는 더 이상 존재하지 않음)

- [ ] **Step 4.5c: Remove stale checklist.md from chain map (lines 71, 81, 85)**

Use `Edit` tool:
- `old_string`: `├── 원칙 변경 → checklist.md (체크리스트에 반영)
`
- `new_string`: ``

Use `Edit` tool:
- `old_string`: `└── 루틴 변경 → checklist.md (체크리스트 항목 갱신)`
- `new_string`: `└── 루틴 변경 → (상위 원칙 연쇄 대상 없음)`

Use `Edit` tool:
- `old_string`: `└── 배포 인프라 변경 → checklist.md (배포 체크리스트 갱신)`
- `new_string`: `└── 배포 인프라 변경 → (상위 원칙 연쇄 대상 없음)`

(연쇄 영향 맵에서 checklist.md 3곳 제거)

- [ ] **Step 4.6: Verify edits**

```bash
grep -n "8개 시스템 문서\|8개 원본 파일\|1~8번 자동 concat" ~/.claude/skills/system-docs-sync/SKILL.md
grep -n "slack.md" ~/.claude/skills/system-docs-sync/SKILL.md
```
Expected: 각 3-4개 매치.

- [ ] **Step 4.7: Commit**

```bash
cd ~/.claude && git add skills/system-docs-sync/SKILL.md && git commit -m "$(cat <<'EOF'
docs(system-docs-sync): 관리 대상에 slack.md 등록 (7→8개)

겸사겸사 stale이었던 checklist.md 제거 + briefing.md 누락 보정.
Slack 허브가 이제 system-docs-sync 연쇄 영향 스킬 관리 범위에 포함됨.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Push Integrated Build to GitHub

**Files:** (read-only execute)

- [ ] **Step 5.1: Run build with --push**

```bash
bash ~/.claude/code/build-integrated_v1.sh --push
```
Expected output 포함:
```
✅ INTEGRATED.md 빌드 완료
=== git push 모드 ===
✅ GitHub push 완료
📎 GitHub raw URL: https://raw.githubusercontent.com/temptation0924-design/claude-system-docs/main/INTEGRATED.md
```

**실패 시 대응 (push 실패 경우)**:
- 네트워크/인증 오류 → `git push` 한 번 더 수동 재시도
- 원격 충돌(non-fast-forward) → `git pull --rebase && git push` 시도
- 3회 실패 시 즉시 매니저(대표님)에게 보고 후 중단 (로컬 커밋 4개는 유지, 다음 세션에서 재push)

- [ ] **Step 5.2: Verify GitHub raw URL contains slack.md content**

```bash
curl -s https://raw.githubusercontent.com/temptation0924-design/claude-system-docs/main/INTEGRATED.md \
  | grep -c "8. slack.md — 슬랙 운영 허브"
```
Expected: **1 이상** (0이면 캐시 지연 — 2분 후 재시도).

---

## Task 6: Final Verification (No Commits)

**Files:** (read-only checks)

- [ ] **Step 6.1: Link integrity final pass**

스펙 §4.3 자동화 스니펫:
```bash
cd ~/.claude && grep -oE '`[a-zA-Z0-9_/.-]+\.md`|`[a-zA-Z0-9_/-]+/`' slack.md \
  | tr -d '`' \
  | while read p; do [ -e "$p" ] || echo "BROKEN: $p"; done
```
Expected: **빈 출력**.

- [ ] **Step 6.2: Count commits created**

```bash
cd ~/.claude && git log --oneline -6 | grep -E "slack|system-docs-sync|integrated"
```
Expected: **5개 커밋** (Task 1, 2, 3, 4, 5의 auto-push commit).

- [ ] **Step 6.3: Report summary**

매니저가 대표님께 최종 보고:
- `slack.md` 생성 (7 섹션)
- CLAUDE.md 라우팅 맵에 "슬랙" 트리거 추가
- INTEGRATED.md 7→8개 concat 파이프라인 정상 작동
- GitHub raw URL에서 slack.md 열람 가능 확인
- 다음 세션에서 "슬랙 채널 뭐 있지?" 테스트 요청

---

## Open Questions (오픈 이슈 — 실행 중 발견 시 보고)

- 해밀시아 rental-inspection 슬랙 알림이 현재 어느 채널로 가는지 확인 필요 → 확인되면 `slack.md` §2 채널 지도에 1행 추가로 후속 PR.
- 본 작업 중 CLAUDE.md에 다른 "N개 md" 언급이 추가로 발견되면 Task 2에 포함.

---

## Self-Review Results

**Spec coverage**:
- §1 배경/목적 → Task 1 slack.md §1에 반영
- §2 위치/라우팅 → Task 2 ✓
- §2.3 "7개→8개" 문구 → Task 2 Step 2.1 ✓
- §3 허브 7 섹션 → Task 1 Step 1.1 ✓
- §4.1 4가지 deliverable → Task 1~5 전부 커버
- §4.3 검증 방법 3종 → Task 6 (링크 무결성) + Task 3.5 (grep concat 결과) + Task 5.2 (GitHub 확인)
- §6 오픈 이슈 → slack.md에 그대로 기재됨 (Task 1)

**Placeholder scan**: 없음. *예약* 마커는 의도적 (채널 미생성 상태).

**Type consistency**: 파일 경로/명령어 모두 절대 경로 통일. Edit old_string이 실제 파일에 존재함을 사전 확인 (Grep 결과와 일치).
