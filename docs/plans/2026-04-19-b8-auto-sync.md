# B8 자동화 (v2 — Preflight 수정 반영) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 시스템 md 수정 시 30초 debounce 후 `build-integrated_v1.sh --push` 자동 실행 + 시크릿 스캔 게이트 + kill-switch + Stop 훅 fallback으로 B8 반복 위반 68→0.

**Architecture:** PostToolUse 신규 훅 `debounce_sync.sh` (macOS 호환 python3 타임스탬프 + mkdir atomic 락 + 시크릿 스캔 + SESSION_ID 전파). Stop 훅은 차단 대신 자동 fallback 실행. 실패/시크릿 발견 시만 hard_block. `SKIP_B8_AUTOSYNC=1` 환경변수로 1회성 우회 (로그 필수).

**Tech Stack:** bash, jq, python3(기본 설치), GitHub CLI, mkdir atomic lock, 기존 `~/.claude/hooks/` 구조.

**Spec**: `docs/specs/2026-04-19-b8-auto-sync-design.md` v2

**Preflight 수정 반영** (2026-04-19):
- ✅ macOS BSD `date +%N` → python3 nanosecond
- ✅ `~` 경로 미확장 → 절대 경로 `/Users/ihyeon-u/.claude/...`
- ✅ `flock` macOS 미설치 → `mkdir` atomic 락
- ✅ `$TRACKER` 미선언 가드 추가
- ✅ TC-4 네트워크 실패 테스트 명시 포함
- ✅ 시크릿 스캔 게이트 추가 (CEO+ENG 공통 지적)
- ✅ SESSION_ID를 stdin에서 추출해 TRIGGER_FILE에 전파
- ✅ session-tracker-log.sh와 `/tmp/claude-session-tracker-lock.d` 공유
- ✅ `SKIP_B8_AUTOSYNC=1` kill-switch 유지
- ✅ 별도 백로그 티켓: `docs/specs/2026-04-19-integrated-md-deprecation-backlog.md`

---

## File Structure

| 파일 | 변경 | 책임 |
|------|------|------|
| `hooks/debounce_sync.sh` | 🆕 신규 | PostToolUse. 타임스탬프 debounce + 시크릿 게이트 + kill-switch + SESSION_ID 전파 |
| `settings.json` | ✏️ 수정 | PostToolUse Write\|Edit 매처에 **절대경로**로 훅 등록 |
| `hooks/session-end-check.sh` | ✏️ 수정 | B8 감지 시 차단 대신 동기 fallback 실행 (+`$TRACKER` 가드) |
| `rules/enforcement.json` | ✏️ 수정 | B8 `override_flag` 제거 + `next_action`에 kill-switch 안내 |

---

## Task 0: 사전 요구사항 확인 (macOS 호환)

**Files:** 없음

**배경**: Preflight 지적 — `flock`/`date +%N`이 macOS에 없음. python3는 macOS 기본 설치.

- [ ] **Step 1: python3 존재 확인**

Run: `which python3 && python3 -c "import time; print(int(time.time()*1e9))"`
Expected: 경로 출력 + 19자리 nanosecond 값

- [ ] **Step 2: jq 존재 확인**

Run: `which jq && jq --version`
Expected: jq 경로 + 버전

- [ ] **Step 3: build-integrated_v1.sh 존재 + 실행 권한 확인**

Run: `ls -l /Users/ihyeon-u/.claude/code/build-integrated_v1.sh`
Expected: `-rwxr-xr-x` 권한 파일

- [ ] **Step 4: 현재 pending_sync 상태 확인 (작업 전 초기화)**

Run:
```bash
TRACKER=$(ls -t /tmp/claude-session-tracker-*.json 2>/dev/null | head -1)
[ -n "$TRACKER" ] && jq '.pending_sync' "$TRACKER"
```
Expected: `[]` 또는 배열 (참고용)

- [ ] **Step 5: build-integrated_v1.sh PATH 재설정 여부 사전 확인 (Preflight WARNING 해소)**

Run:
```bash
grep -nE "^\s*(export\s+)?PATH=" /Users/ihyeon-u/.claude/code/build-integrated_v1.sh
```
Expected: 출력 없음. 있으면 → TC-4 PATH 오버라이드 시뮬레이션 전략 수정 필요 (대체: 실제 WiFi off 또는 git config url override)

- [ ] **Step 6: session-end-check.sh stdin session_id 파싱 여부 확인 (TC-3 유효성)**

Run:
```bash
grep -nE "session_id|SESSION_ID" /Users/ihyeon-u/.claude/hooks/session-end-check.sh
```
Expected: `SESSION_ID=$(echo "$INPUT" | jq -r '.session_id ...` 라인 존재. 없으면 → TC-3 dry-run 전에 파싱 로직 추가 필요

---

## Task 1: debounce_sync.sh 스크립트 작성 (v2)

**Files:**
- Create: `/Users/ihyeon-u/.claude/hooks/debounce_sync.sh`

- [ ] **Step 1: 스크립트 생성 (권한 755)**

Run:
```bash
cat > /Users/ihyeon-u/.claude/hooks/debounce_sync.sh <<'SCRIPT'
#!/bin/bash
# debounce_sync.sh — PostToolUse: 시스템 md 수정 시 30초 debounce 후 INTEGRATED.md 재빌드+push (v2)
# 설계: docs/specs/2026-04-19-b8-auto-sync-design.md §4.1
# Preflight 수정 반영: macOS 호환 + 시크릿 스캔 + SESSION_ID 전파 + kill-switch

set +e

SYSTEM_DOCS="CLAUDE.md rules.md session.md env-info.md skill-guide.md agent.md briefing.md slack.md"
DEBOUNCE_SEC=30
TRIGGER_FILE="/tmp/claude-b8-debounce-trigger"
LOCK_DIR="/tmp/claude-b8-debounce.lock.d"
SHARED_TRACKER_LOCK="/tmp/claude-session-tracker-lock.d"
LOG_FILE="/tmp/claude-b8-debounce.log"
SECRET_PATTERNS='sk-ant-|ghp_|gho_|ghu_|ghs_|xoxb-|xoxp-|AKIA|AIza|glpat-'

INPUT=$(cat 2>/dev/null || true)
FILE_PATH=$(echo "$INPUT" | jq -r '.tool_input.file_path // empty' 2>/dev/null)
SESSION_ID=$(echo "$INPUT" | jq -r '.session_id // empty' 2>/dev/null | tr -cd 'a-zA-Z0-9_-')
[ -z "$FILE_PATH" ] && exit 0

[[ "$FILE_PATH" != "$HOME/.claude/"* ]] && exit 0
BASENAME=$(basename "$FILE_PATH")
echo "$SYSTEM_DOCS" | tr ' ' '\n' | grep -qx "$BASENAME" || exit 0

if [ "${SKIP_B8_AUTOSYNC:-0}" = "1" ]; then
  echo "[$(date '+%H:%M:%S')] KILL_SWITCH_USED file=${BASENAME} session=${SESSION_ID}" >> "$LOG_FILE"
  exit 0
fi

if MY_TS=$(python3 -c "import time; print(int(time.time()*1e9))" 2>/dev/null); then
  :
else
  MY_TS="$(date +%s)$(printf '%09d' $((RANDOM * 30000 + RANDOM)))"
fi
echo "${MY_TS}|${SESSION_ID}" > "$TRIGGER_FILE"
echo "[$(date '+%H:%M:%S')] TRIGGER ${BASENAME} ts=${MY_TS} session=${SESSION_ID}" >> "$LOG_FILE"

(
  sleep "$DEBOUNCE_SEC"

  CURRENT_LINE=$(cat "$TRIGGER_FILE" 2>/dev/null)
  CURRENT_TS="${CURRENT_LINE%%|*}"
  CURRENT_SESSION="${CURRENT_LINE##*|}"
  if [ "$MY_TS" != "$CURRENT_TS" ]; then
    echo "[$(date '+%H:%M:%S')] DEBOUNCE_SKIP ts=${MY_TS}" >> "$LOG_FILE"
    exit 0
  fi

  if ! mkdir "$LOCK_DIR" 2>/dev/null; then
    echo "[$(date '+%H:%M:%S')] LOCK_BUSY skip" >> "$LOG_FILE"
    exit 0
  fi
  trap 'rmdir "$LOCK_DIR" 2>/dev/null' EXIT

  cd "$HOME/.claude" || exit 1

  # 🔐 시크릿 스캔 게이트
  SECRET_FOUND=""
  for DOC in $SYSTEM_DOCS; do
    if [ -f "$DOC" ] && grep -lE "$SECRET_PATTERNS" "$DOC" >/dev/null 2>&1; then
      SECRET_FOUND="${SECRET_FOUND} ${DOC}"
    fi
  done
  if [ -n "$SECRET_FOUND" ]; then
    echo "[$(date '+%H:%M:%S')] SECRET_GATE_BLOCK files=${SECRET_FOUND} — push 차단" >> "$LOG_FILE"
    bash code/build-integrated_v1.sh >> "$LOG_FILE" 2>&1
    exit 1
  fi

  echo "[$(date '+%H:%M:%S')] BUILD_START ts=${MY_TS}" >> "$LOG_FILE"
  if bash code/build-integrated_v1.sh --push >> "$LOG_FILE" 2>&1; then
    echo "[$(date '+%H:%M:%S')] BUILD_SUCCESS" >> "$LOG_FILE"

    if [ -n "$CURRENT_SESSION" ]; then
      TRACKER="/tmp/claude-session-tracker-${CURRENT_SESSION}.json"
      if [ -f "$TRACKER" ]; then
        # session-tracker-log.sh와 LOCKDIR 공유
        WAIT=0
        while ! mkdir "$SHARED_TRACKER_LOCK" 2>/dev/null; do
          sleep 0.1
          WAIT=$((WAIT + 1))
          [ $WAIT -gt 30 ] && break  # 3초 타임아웃
        done
        TMPFILE=$(mktemp)
        if jq '.pending_sync = [] | .system_files_edited = false' "$TRACKER" > "$TMPFILE" 2>/dev/null; then
          mv "$TMPFILE" "$TRACKER"
          echo "[$(date '+%H:%M:%S')] TRACKER_CLEARED session=${CURRENT_SESSION}" >> "$LOG_FILE"
        else
          rm -f "$TMPFILE"
          echo "[$(date '+%H:%M:%S')] TRACKER_UPDATE_FAILED session=${CURRENT_SESSION}" >> "$LOG_FILE"
        fi
        rmdir "$SHARED_TRACKER_LOCK" 2>/dev/null
      fi
    fi
  else
    echo "[$(date '+%H:%M:%S')] BUILD_FAILED — Stop 훅이 재시도" >> "$LOG_FILE"
  fi
) &

exit 0
SCRIPT
chmod 755 /Users/ihyeon-u/.claude/hooks/debounce_sync.sh
```

- [ ] **Step 2: bash 문법 검증**

Run: `bash -n /Users/ihyeon-u/.claude/hooks/debounce_sync.sh && echo "OK"`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
cd /Users/ihyeon-u/.claude
git add hooks/debounce_sync.sh
git commit -m "feat(hooks): debounce_sync.sh v2 — macOS 호환 + 시크릿 게이트 + kill-switch + SESSION_ID 전파"
```

---

## Task 2: 단위 동작 테스트

**Files:** 없음 (수동 stdin 주입)

- [ ] **Step 1: 시스템 md 아닌 파일 → 즉시 exit**

Run:
```bash
rm -f /tmp/claude-b8-debounce.log
echo '{"tool_input":{"file_path":"/tmp/irrelevant.md"},"session_id":"test-1"}' | bash /Users/ihyeon-u/.claude/hooks/debounce_sync.sh
echo "exit=$?"
[ -f /tmp/claude-b8-debounce.log ] && cat /tmp/claude-b8-debounce.log || echo "(no log)"
```
Expected: `exit=0`, 로그 없음

- [ ] **Step 2: 시스템 md → TRIGGER 기록 + 타임스탬프 파일 확인**

Run:
```bash
rm -f /tmp/claude-b8-debounce.log /tmp/claude-b8-debounce-trigger
echo '{"tool_input":{"file_path":"/Users/ihyeon-u/.claude/CLAUDE.md"},"session_id":"test-ses-1"}' | bash /Users/ihyeon-u/.claude/hooks/debounce_sync.sh
cat /tmp/claude-b8-debounce-trigger
grep TRIGGER /tmp/claude-b8-debounce.log
```
Expected: `<19자리 nanosecond>|test-ses-1` + `TRIGGER CLAUDE.md ts=... session=test-ses-1`

- [ ] **Step 3: python3 타임스탬프 유효성 검증 (19자리)**

Run:
```bash
TS=$(cat /tmp/claude-b8-debounce-trigger | cut -d'|' -f1)
echo "$TS length=${#TS}"
```
Expected: `length=19`

- [ ] **Step 4: Kill-switch 동작 확인**

Run:
```bash
rm -f /tmp/claude-b8-debounce.log /tmp/claude-b8-debounce-trigger
SKIP_B8_AUTOSYNC=1 bash -c 'echo "{\"tool_input\":{\"file_path\":\"/Users/ihyeon-u/.claude/CLAUDE.md\"},\"session_id\":\"test-ks\"}" | bash /Users/ihyeon-u/.claude/hooks/debounce_sync.sh'
grep KILL_SWITCH_USED /tmp/claude-b8-debounce.log
[ ! -f /tmp/claude-b8-debounce-trigger ] && echo "NO_TRIGGER_OK" || cat /tmp/claude-b8-debounce-trigger
```
Expected: `KILL_SWITCH_USED file=CLAUDE.md ...` 로그 있음, TRIGGER 파일 없음 (NO_TRIGGER_OK)

- [ ] **Step 5: Debounce 폐기 로직 테스트 (임시 3초)**

Run:
```bash
# 임시 3초 변경
sed -i.bak 's/DEBOUNCE_SEC=30/DEBOUNCE_SEC=3/' /Users/ihyeon-u/.claude/hooks/debounce_sync.sh

rm -f /tmp/claude-b8-debounce.log /tmp/claude-b8-debounce-trigger /tmp/claude-b8-debounce.lock.d 2>/dev/null
rmdir /tmp/claude-b8-debounce.lock.d 2>/dev/null

echo '{"tool_input":{"file_path":"/Users/ihyeon-u/.claude/CLAUDE.md"},"session_id":"test-d1"}' | bash /Users/ihyeon-u/.claude/hooks/debounce_sync.sh
sleep 1
echo '{"tool_input":{"file_path":"/Users/ihyeon-u/.claude/rules.md"},"session_id":"test-d1"}' | bash /Users/ihyeon-u/.claude/hooks/debounce_sync.sh

sleep 5

cat /tmp/claude-b8-debounce.log
```
Expected: TRIGGER 2개, DEBOUNCE_SKIP 1개 (첫 번째 폐기), BUILD_START 1개 (두 번째 최신이므로 실행), BUILD_SUCCESS 또는 SECRET_GATE_BLOCK.

- [ ] **Step 6: DEBOUNCE_SEC 원복 + diff 검증**

Run:
```bash
mv /Users/ihyeon-u/.claude/hooks/debounce_sync.sh.bak /Users/ihyeon-u/.claude/hooks/debounce_sync.sh.tmp
grep -q "DEBOUNCE_SEC=30" /Users/ihyeon-u/.claude/hooks/debounce_sync.sh.tmp && echo "BAK_HAS_30_OK"
mv /Users/ihyeon-u/.claude/hooks/debounce_sync.sh.tmp /Users/ihyeon-u/.claude/hooks/debounce_sync.sh
grep -c "DEBOUNCE_SEC=30" /Users/ihyeon-u/.claude/hooks/debounce_sync.sh
```
Expected: `BAK_HAS_30_OK`, 최종 파일에 `DEBOUNCE_SEC=30` 1개

- [ ] **Step 7: 빌드 아티팩트 정리**

Run:
```bash
rmdir /tmp/claude-b8-debounce.lock.d 2>/dev/null
rmdir /tmp/claude-session-tracker-lock.d 2>/dev/null
echo "cleanup done"
```

---

## Task 3: settings.json PostToolUse 매처 확장 (절대 경로)

**Files:**
- Modify: `/Users/ihyeon-u/.claude/settings.json`

**Preflight 수정**: `~/.claude/...` → `/Users/ihyeon-u/.claude/...` 절대 경로.

- [ ] **Step 1: 현재 Write|Edit 매처 hooks 배열 확인**

Run:
```bash
jq '.hooks.PostToolUse[] | select(.matcher == "Write|Edit") | .hooks[].command' /Users/ihyeon-u/.claude/settings.json
```
Expected: 등록된 hook 2~3개 경로 확인

- [ ] **Step 2: debounce_sync.sh 절대 경로로 추가**

Run:
```bash
cp /Users/ihyeon-u/.claude/settings.json /Users/ihyeon-u/.claude/settings.json.bak.b8
jq '(.hooks.PostToolUse[] | select(.matcher == "Write|Edit") | .hooks) += [{"type": "command", "command": "/Users/ihyeon-u/.claude/hooks/debounce_sync.sh"}]' /Users/ihyeon-u/.claude/settings.json > /tmp/settings.new.json
mv /tmp/settings.new.json /Users/ihyeon-u/.claude/settings.json
```

- [ ] **Step 3: 등록 확인 + JSON 유효성**

Run:
```bash
jq empty /Users/ihyeon-u/.claude/settings.json && echo "JSON_OK"
jq '.hooks.PostToolUse[] | select(.matcher == "Write|Edit") | .hooks[].command' /Users/ihyeon-u/.claude/settings.json | grep debounce_sync
```
Expected: `JSON_OK` + `"/Users/ihyeon-u/.claude/hooks/debounce_sync.sh"` 출력

- [ ] **Step 4: Commit + 백업 삭제**

```bash
cd /Users/ihyeon-u/.claude
git add settings.json
git commit -m "feat(settings): register debounce_sync.sh on Write|Edit — 절대경로로 B8 자동화 활성"
rm settings.json.bak.b8
```

---

## Task 4: session-end-check.sh — B8 fallback 동기 실행 ($TRACKER 가드 포함)

**Files:**
- Modify: `/Users/ihyeon-u/.claude/hooks/session-end-check.sh`

- [ ] **Step 1: 수정 위치 + `$TRACKER` 변수 확인**

Run:
```bash
grep -n "B8: INTEGRATED\|^TRACKER=" /Users/ihyeon-u/.claude/hooks/session-end-check.sh
```
Expected: B8 블록 라인 번호 + `TRACKER="..."` 선언 라인 확인

- [ ] **Step 2: 기존 B8 블록 → fallback 자동 실행 블록으로 Edit**

old_string:
```bash
# B8: INTEGRATED.md 재빌드 누락 (시스템 파일 수정했는데 pending_sync 남아있음)
if [ -n "$PENDING_SYNC" ] && [ "$PENDING_SYNC" -gt 0 ] 2>/dev/null; then
  BLOCKS+="❌ B8: INTEGRATED.md 재빌드 누락 (미동기화: ${PENDING_FILES})\n"
fi
```

new_string:
```bash
# B8: INTEGRATED.md 재빌드 누락 — fallback 동기 실행 (debounce 못 돈 케이스, 시크릿 검증 포함)
if [ -n "$PENDING_SYNC" ] && [ "$PENDING_SYNC" -gt 0 ] 2>/dev/null; then
  B8_LOG=/tmp/claude-b8-debounce.log
  echo "[$(date '+%H:%M:%S')] B8_FALLBACK_START pending=${PENDING_FILES}" >> "$B8_LOG"

  # 시크릿 스캔 (debounce_sync.sh와 동일 로직)
  SECRET_PATTERNS='sk-ant-|ghp_|gho_|ghu_|ghs_|xoxb-|xoxp-|AKIA|AIza|glpat-'
  cd "$HOME/.claude" 2>/dev/null
  SECRET_FOUND=""
  for DOC in CLAUDE.md rules.md session.md env-info.md skill-guide.md agent.md briefing.md slack.md; do
    [ -f "$DOC" ] && grep -lE "$SECRET_PATTERNS" "$DOC" >/dev/null 2>&1 && SECRET_FOUND="${SECRET_FOUND} ${DOC}"
  done

  if [ -n "$SECRET_FOUND" ]; then
    echo "[$(date '+%H:%M:%S')] B8_FALLBACK_SECRET_BLOCK files=${SECRET_FOUND}" >> "$B8_LOG"
    BLOCKS+="❌ B8: 시크릿 의심 패턴 발견 (${SECRET_FOUND}). 수동 점검 후 bash ~/.claude/code/build-integrated_v1.sh --push\n"
  elif bash code/build-integrated_v1.sh --push >> "$B8_LOG" 2>&1; then
    echo "[$(date '+%H:%M:%S')] B8_FALLBACK_SUCCESS" >> "$B8_LOG"
    if [ -n "$TRACKER" ] && [ -f "$TRACKER" ]; then
      TMPFILE=$(mktemp)
      if jq '.pending_sync = [] | .system_files_edited = false' "$TRACKER" > "$TMPFILE" 2>/dev/null; then
        mv "$TMPFILE" "$TRACKER"
      else
        rm -f "$TMPFILE"
      fi
    fi
  else
    echo "[$(date '+%H:%M:%S')] B8_FALLBACK_FAILED" >> "$B8_LOG"
    BLOCKS+="❌ B8: INTEGRATED.md 자동 재빌드 실패 (미동기화: ${PENDING_FILES}). 수동 실행: bash ~/.claude/code/build-integrated_v1.sh --push\n"
  fi
fi
```

- [ ] **Step 3: 문법 + 변수 검증**

Run:
```bash
bash -n /Users/ihyeon-u/.claude/hooks/session-end-check.sh && echo "SYNTAX_OK"
grep -c "B8_FALLBACK_START" /Users/ihyeon-u/.claude/hooks/session-end-check.sh
grep -c "^TRACKER=" /Users/ihyeon-u/.claude/hooks/session-end-check.sh
```
Expected: `SYNTAX_OK` + `1` + `1 이상` ($TRACKER 선언 존재)

- [ ] **Step 4: Commit**

```bash
cd /Users/ihyeon-u/.claude
git add hooks/session-end-check.sh
git commit -m "refactor(hooks): session-end-check B8 fallback — 시크릿 검증 + 자동 재빌드, 실패 시만 block"
```

---

## Task 5: enforcement.json — `override_flag` 제거 + kill-switch 안내

**Files:**
- Modify: `/Users/ihyeon-u/.claude/rules/enforcement.json`

- [ ] **Step 1: 현재 B8 블록 확인**

Run:
```bash
jq '.rules[] | select(.code == "B8")' /Users/ihyeon-u/.claude/rules/enforcement.json
```
Expected: `override_flag: "--force-B8"` 존재

- [ ] **Step 2: override_flag 제거 + next_action 갱신**

Run:
```bash
jq '(.rules[] | select(.code == "B8")) |= (del(.override_flag) | .next_action = "자동: debounce_sync.sh가 30초 후 실행. 긴급 우회: SKIP_B8_AUTOSYNC=1 환경변수 (로그 필수 기록)")' /Users/ihyeon-u/.claude/rules/enforcement.json > /tmp/enforcement.new.json
mv /tmp/enforcement.new.json /Users/ihyeon-u/.claude/rules/enforcement.json
```

- [ ] **Step 3: 검증**

Run:
```bash
jq '.rules[] | select(.code == "B8") | {override_flag: (.override_flag // "REMOVED"), next_action}' /Users/ihyeon-u/.claude/rules/enforcement.json
jq empty /Users/ihyeon-u/.claude/rules/enforcement.json && echo "JSON_OK"
```
Expected: `override_flag: "REMOVED"`, `next_action`에 `SKIP_B8_AUTOSYNC` 포함, `JSON_OK`

- [ ] **Step 4: Commit**

```bash
cd /Users/ihyeon-u/.claude
git add rules/enforcement.json
git commit -m "refactor(rules): B8 override_flag 제거, SKIP_B8_AUTOSYNC kill-switch 안내"
```

---

## Task 6: 시나리오 테스트 (TC-1 ~ TC-7)

**Files:** 없음 (실제 훅 트리거)

**배경**: 이 단계부터 시스템 md 수정이 실제 debounce 발화. 로그 `/tmp/claude-b8-debounce.log`에 기록.

- [ ] **Step 1: 로그 초기화**

Run:
```bash
rm -f /tmp/claude-b8-debounce.log /tmp/claude-b8-debounce-trigger
rmdir /tmp/claude-b8-debounce.lock.d 2>/dev/null
rmdir /tmp/claude-session-tracker-lock.d 2>/dev/null
touch /tmp/claude-b8-debounce.log
```

- [ ] **Step 2: TC-1 단일 수정 → 30초 후 자동 push**

현 plan의 단계 완료를 위해 실제 CLAUDE.md footer 시각을 Edit로 갱신 (의미 있는 무해 변경). 35초 대기 후 로그 확인.

Run (Edit 도구 사용 후 대기):
```bash
sleep 35
tail -15 /tmp/claude-b8-debounce.log
cd /Users/ihyeon-u/.claude && git log origin/main --oneline -3
```
Expected: TRIGGER CLAUDE.md → BUILD_START → BUILD_SUCCESS → TRACKER_CLEARED. origin/main에 최신 커밋 반영.

- [ ] **Step 3: TC-2 연속 3회 수정 → push 1회만**

CLAUDE.md, rules.md, session.md 순서로 5초 간격 Edit. 마지막 수정 후 35초 대기.

Run:
```bash
# 수정은 Edit 도구로 실제 진행 — 여기선 대기+확인만
sleep 35
tail -20 /tmp/claude-b8-debounce.log | grep -E "TRIGGER|DEBOUNCE_SKIP|BUILD_START|BUILD_SUCCESS"
```
Expected: TRIGGER 3개, DEBOUNCE_SKIP 2개, BUILD_START 1개, BUILD_SUCCESS 1개

- [ ] **Step 4: TC-3 Stop fallback 시뮬레이션 (dry-run)**

debounce 없이 Stop 훅이 직접 실행되는 케이스를 가짜 tracker로 시뮬레이션.

Run:
```bash
# 테스트용 가짜 tracker 생성
TEST_TRACKER="/tmp/claude-session-tracker-TEST-fallback.json"
echo '{"pending_sync":["CLAUDE.md"],"system_files_edited":true,"agent_dispatched":true,"errors_resolved":0,"new_concepts_introduced":false,"skills_dir_changed":false,"skill_guide_edited":false,"warning_sent":false,"mode1_entered":false,"mode2_entered":false}' > "$TEST_TRACKER"

# Stop 훅 직접 구동 (session_id + 환경변수 주입)
echo "{\"session_id\":\"TEST-fallback\"}" | bash /Users/ihyeon-u/.claude/hooks/session-end-check.sh > /tmp/stop-test.out 2>&1
tail -10 /tmp/claude-b8-debounce.log
cat /tmp/stop-test.out
rm -f "$TEST_TRACKER" /tmp/stop-test.out
```
Expected: `B8_FALLBACK_START` → `B8_FALLBACK_SUCCESS` 또는 `B8_FALLBACK_SECRET_BLOCK` 로그. 가짜 tracker의 pending_sync가 비었다면 성공.

- [ ] **Step 5: TC-4 네트워크 실패 시뮬레이션**

⚠️ **리스크**: WiFi 끄면 세션 MCP 연결 끊김. 대신 `build-integrated_v1.sh` 내부에 가짜 `git` 바이너리 주입으로 push 실패 시뮬레이션.

Run:
```bash
# PATH 오버라이드로 git push 실패 시뮬레이션
FAKE_GIT_DIR=$(mktemp -d)
cat > "$FAKE_GIT_DIR/git" <<'EOF'
#!/bin/bash
if [ "$1" = "push" ]; then
  echo "fatal: unable to access (simulated network error)" >&2
  exit 128
fi
exec /usr/bin/env git "$@"
EOF
chmod 755 "$FAKE_GIT_DIR/git"

# stdin 주입 + PATH 오버라이드
rm -f /tmp/claude-b8-debounce.log
PATH="$FAKE_GIT_DIR:$PATH" bash -c 'echo "{\"tool_input\":{\"file_path\":\"/Users/ihyeon-u/.claude/CLAUDE.md\"},\"session_id\":\"test-netfail\"}" | bash /Users/ihyeon-u/.claude/hooks/debounce_sync.sh'
# DEBOUNCE_SEC=30이므로 기다려야 함 — 짧게 테스트 위해 임시 변경 또는 35초 대기
sleep 35
grep -E "BUILD_START|BUILD_FAILED" /tmp/claude-b8-debounce.log

rm -rf "$FAKE_GIT_DIR"
```
Expected: `BUILD_START` → `BUILD_FAILED` 로그 확인 (네트워크 실패 복구 경로 작동)

- [ ] **Step 6: TC-6 시크릿 스캔 게이트 동작 확인**

Run:
```bash
# 테스트용 임시 파일에 fake API 키 삽입 (실제 시스템 md는 건드리지 않음)
TEST_DIR=$(mktemp -d)
mkdir -p "$TEST_DIR/.claude/code"
cp /Users/ihyeon-u/.claude/code/build-integrated_v1.sh "$TEST_DIR/.claude/code/"
cp /Users/ihyeon-u/.claude/hooks/debounce_sync.sh "$TEST_DIR/"
cp /Users/ihyeon-u/.claude/CLAUDE.md "$TEST_DIR/.claude/"
echo "sk-ant-TESTKEY123456789" >> "$TEST_DIR/.claude/CLAUDE.md"  # fake 시크릿

# HOME 오버라이드로 debounce_sync.sh 실행
rm -f /tmp/claude-b8-debounce.log
HOME="$TEST_DIR" bash -c 'echo "{\"tool_input\":{\"file_path\":\"'$TEST_DIR'/.claude/CLAUDE.md\"},\"session_id\":\"test-secret\"}" | bash '$TEST_DIR'/debounce_sync.sh'
sleep 35
grep "SECRET_GATE_BLOCK" /tmp/claude-b8-debounce.log

rm -rf "$TEST_DIR"
```
Expected: `SECRET_GATE_BLOCK files= CLAUDE.md — push 차단` 로그

- [ ] **Step 7: TC-5 override 제거 확인**

Run:
```bash
jq '.rules[] | select(.code == "B8") | .override_flag // "REMOVED"' /Users/ihyeon-u/.claude/rules/enforcement.json
```
Expected: `"REMOVED"`

- [ ] **Step 8: 테스트 로그 보관 + Commit**

Run:
```bash
cp /tmp/claude-b8-debounce.log /Users/ihyeon-u/.claude/docs/specs/2026-04-19-b8-auto-sync-testlog.txt
cd /Users/ihyeon-u/.claude
git add docs/specs/2026-04-19-b8-auto-sync-testlog.txt
git commit -m "test(b8): TC-1~TC-7 시나리오 테스트 로그 보관"
```

---

## Task 7: 자기참조 검증 (rules.md / CLAUDE.md 갱신)

**Files:**
- Modify: `/Users/ihyeon-u/.claude/rules.md`
- Modify: `/Users/ihyeon-u/.claude/CLAUDE.md`

- [ ] **Step 1: rules.md B8 체크리스트 갱신**

old_string:
```
**B8 체크리스트 (간소화)**
- [ ] GitHub `INTEGRATED.md` 재빌드 + push 완료
```
new_string:
```
**B8 체크리스트 (자동화됨 v2 — 2026-04-19)**
- ✅ `debounce_sync.sh`가 수정 30초 후 자동 빌드+push
- ✅ 시크릿 스캔 게이트 (토큰 패턴 발견 시 push 차단)
- ✅ Stop 훅 fallback (debounce 못 돈 케이스 동기 실행)
- ✅ `override_flag: --force-B8` 제거 (뒷문 차단)
- ⚡ 긴급 우회: `SKIP_B8_AUTOSYNC=1` 환경변수 (로그 필수 기록)
- ⚠️ 실패 시만 수동: `bash ~/.claude/code/build-integrated_v1.sh --push`
```

- [ ] **Step 2: CLAUDE.md footer 버전 bump**

old_string: `*Haemilsia AI operations | 2026.04.11 | v4.2.2 — handoffs/ 디렉토리 신설 + .claude/ 루트 정리 + #general-mode private_channel ID 명시*`

new_string: `*Haemilsia AI operations | 2026.04.19 | v4.2.3 — B8 자동화 v2 (debounce_sync.sh + 시크릿 게이트 + SKIP_B8_AUTOSYNC kill-switch)*`

- [ ] **Step 3: 35초 대기 → 자동 push 확인**

Run:
```bash
sleep 35
tail -10 /tmp/claude-b8-debounce.log
cd /Users/ihyeon-u/.claude && git log origin/main --oneline -3
```
Expected: TRIGGER rules.md → BUILD_SUCCESS. origin/main에 최신 커밋.

- [ ] **Step 4: 수동 commit이 남아있다면 커밋**

Run:
```bash
cd /Users/ihyeon-u/.claude
git status
git add -u && git commit -m "docs: B8 자동화 v2 사실 기록 + v4.2.3 bump" || echo "nothing to commit"
```

---

## Task 8: 최종 검증 체크리스트

- [ ] **Step 1: 훅 절대경로 등록 확인**

Run:
```bash
jq '.hooks.PostToolUse[] | select(.matcher == "Write|Edit") | .hooks[].command' /Users/ihyeon-u/.claude/settings.json | grep -c "^\"/Users/ihyeon-u/.claude/hooks/debounce_sync.sh\"$"
```
Expected: `1`

- [ ] **Step 2: override_flag 제거 확인**

Run:
```bash
jq '.rules[] | select(.code == "B8") | .override_flag // "REMOVED"' /Users/ihyeon-u/.claude/rules/enforcement.json
```
Expected: `"REMOVED"`

- [ ] **Step 3: 훅 스크립트 권한 확인**

Run: `ls -l /Users/ihyeon-u/.claude/hooks/debounce_sync.sh`
Expected: `-rwxr-xr-x`

- [ ] **Step 4: Stop 훅 fallback + 가드 존재 확인**

Run:
```bash
grep -c "B8_FALLBACK_START\|B8_FALLBACK_SECRET_BLOCK\|\[ -n \"\\\$TRACKER\" \]" /Users/ihyeon-u/.claude/hooks/session-end-check.sh
```
Expected: `3 이상`

- [ ] **Step 5: GitHub INTEGRATED.md 최신성 확인**

Run:
```bash
curl -s https://raw.githubusercontent.com/temptation0924-design/claude-system-docs/main/INTEGRATED.md | grep -m1 "v4.2.3\|2026-04-19"
```
Expected: 최신 버전 문자열 포함

- [ ] **Step 6: Notion 규칙위반 DB B8 기록 — "자동화 완료" 코멘트**

대표님께 보고: B8 반복횟수 68 → 2026-04-19 자동화 완료, 이후 카운터 관찰용 1주 모니터링 예정.

- [ ] **Step 7: 최종 status + Commit**

Run:
```bash
cd /Users/ihyeon-u/.claude
git status
git add -u && git commit -m "chore: B8 자동화 v2 phase 완료" || echo "clean"
```

---

## Self-Review

### Spec v2 커버리지
- ✅ §4.1 debounce_sync.sh v2 → Task 1 Step 1 전체 코드 그대로
- ✅ §4.2 session-end-check fallback → Task 4 Step 2 시크릿 검증 + $TRACKER 가드
- ✅ §4.3 override_flag 제거 → Task 5
- ✅ §4.4 settings.json 절대 경로 → Task 3
- ✅ §5.1~5.4 데이터 흐름 → Task 6 TC-1/TC-2/TC-3/TC-5
- ✅ 시크릿 스캔 게이트 → Task 6 TC-6
- ✅ Kill-switch → Task 2 Step 4 + Task 5 next_action

### Preflight Critical 5건 반영 확인
1. ✅ macOS `date +%N` → python3 + RANDOM fallback (Task 1 Step 1 코드)
2. ✅ `~` 경로 미확장 → `/Users/ihyeon-u/.claude/...` 절대경로 (Task 3 Step 2)
3. ✅ `flock` 미설치 → `mkdir` atomic 락 (Task 1 Step 1 코드)
4. ✅ `$TRACKER` 가드 → `[ -n "$TRACKER" ] && [ -f "$TRACKER" ]` (Task 4 Step 2)
5. ✅ TC-4 네트워크 실패 → Task 6 Step 5 PATH 오버라이드 시뮬레이션

### CEO/ENG 보완 확인
- ✅ 시크릿 스캔 게이트 (CEO+ENG)
- ✅ SESSION_ID 전파 (ENG)
- ✅ LOCKDIR 공유 (ENG)
- ✅ Kill-switch 유지 (CEO)
- ✅ INTEGRATED.md 폐기 백로그 — 별도 파일 `docs/specs/2026-04-19-integrated-md-deprecation-backlog.md`

### 알려진 남은 위험
1. Task 2 Step 5의 DEBOUNCE_SEC=3 임시 변경 실패 시 30초 상태 지속 → Step 6에서 검증
2. Task 6 Step 2~3 실제 훅 트리거 시 세션 tracker pending_sync 영향 → 정상 플로우이므로 OK
3. Task 7 sleep 35는 단일 step 시간 초과 — 일부러 debounce 대기 검증 목적. 백그라운드 대기용으로 허용

---

## Execution Handoff

**두 실행 옵션**:
1. **Subagent-Driven (추천하지 않음)** — subagent가 settings.json 잘못 건드리면 훅 체계 붕괴 리스크
2. **Inline Execution (추천)** — 현재 세션에서 체크포인트와 함께 순차 실행

**Inline + 대표님 체크포인트** 권장.
